import {createTheme} from "@mui/material";

const theme = createTheme({
  palette: {},
  typography: {
    fontFamily: [
      'Roboto',
      'sans-serif',
    ].join(','),
  }
});

export default theme;